import { Card } from 'antd';
import styled from 'styled-components';

export const CreditReportStyled = styled(Card)`
  .header {
    display: flex;
    justify-content: center;
    align-items: center;
    font-weight: bold;
  }
`;
