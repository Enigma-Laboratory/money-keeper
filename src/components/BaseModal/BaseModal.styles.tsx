import { Modal } from 'antd';
import styled from 'styled-components';

export const StyledBaseModal = styled(Modal)`
  .ant-modal-content {
    .header {
    }
    .ant-modal-footer {
      text-align: center;
    }
  }
`;
